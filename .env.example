# Environment variables template for Sih-proto
# IMPORTANT: Do not commit real secrets. Copy the respective section into the target app's .env file.
#
# Backend .env path:   sihOceanBackend/.env
# Frontend .env path:  sihOceanFrontend/sihOcean/.env

########################################
# Backend (sihOceanBackend/.env)
########################################
# Supabase
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_supabase_anon_key

# Server
PORT=5000
NODE_ENV=development

########################################
# Frontend (sihOceanFrontend/sihOcean/.env)
########################################
# Backend API base URL (for the browser)
VITE_API_BASE=http://localhost:5000

# Third-party APIs used by the frontend
# WeatherAPI.com key (used in App.jsx)
VITE_WEATHERAPI_KEY=your_weatherapi_key

# Sarvam API key (used in src/utils/i18n.js)
VITE_SARVAM_API_KEY=your_sarvam_api_key

########################################
# Notes
########################################
# - Vite only exposes variables prefixed with VITE_ to the browser.
# - The backend reads variables via process.env (Node/dotenv).